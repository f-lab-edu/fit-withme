<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.fitwithme.infrastructure.mapper.TestMapper">
  <select id="find" parameterType="Long" resultType="com.example.fitwithme.domain.model.Test">
    SELECT
      id,
      name,
      age,
      address
    FROM
      person
    WHERE
      id = #{id}
  </select>


  <insert id="create" parameterType="com.example.fitwithme.domain.model.Test" >
    INSERT INTO
      person
    (
      id,
      name,
      age,
      address
    )
    VALUES
      (
        #{id},
        #{name},
        #{age},
        #{address}
      )
  </insert>
</mapper>
